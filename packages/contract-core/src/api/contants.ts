export const CONTRACT_PRELOADED_ENTRIES = 'we:preloaded_entries'
export const VAR_META = 'we:var:meta'
export const CONTRACT_VARS = 'we:contract:vars'


export const ACTION_METADATA = 'we:contract:actions'
export const ARGS_METADATA = 'we:contract:args'

export enum ReservedParamNames {
  action = 'action',
}

export enum TxId {
  call = 104,
  create = 103,
}
